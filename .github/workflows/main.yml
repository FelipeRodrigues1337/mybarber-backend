name: CI
on:
  push:
    branches: [ master ]
    
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    steps:
 
      - uses: actions/checkout@v2
      
      - name: Setup Node.js environment
        uses: actions/setup-node@v2.4.1
        with:
          node-version: 14.x
          
      #Instalar dependencias NPM/YARN   
      - name: install dependencies
        run: yarn
        
      #Executar build 
      - name: run build
        run: yarn build
        
      - name: copy build to digital ocean
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{secrets.SSH_KEY}}
          source: "."
          target: "/home/nodeproject/"  

     #Instalar dependencias NPM/YARN
     #Executar build
     # Copiar pasta BUILD para dentro da Digital Ocean
     # Substituir o código existente na pasta "build" pelo novo código
     # Executar "yarn" na digital ocean
     # Executar migrations typeorm
     #Restart node server.
